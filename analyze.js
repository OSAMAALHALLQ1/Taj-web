const fs=require('fs');const content=fs.readFileSync('target.svg', 'utf-8');const paths=content.match(/<path[^>]+>/g)||[];paths.forEach((p,i)=>{const m=p.match(/d="([^"]+)"/);if(m){const coords=m[1].match(/-?\d+\.?\d*/g);if(coords){const nums=coords.map(Number);let minX=Infinity,maxX=-Infinity,minY=Infinity,maxY=-Infinity;for(let j=0;j<nums.length;j+=2){minX=Math.min(minX,nums[j]);maxX=Math.max(maxX,nums[j]);minY=Math.min(minY,nums[j+1]);maxY=Math.max(maxY,nums[j+1]);}console.log(Path : w= h= x= y=);}}});console.log('rects:', content.match(/<rect[^>]+>/g));
